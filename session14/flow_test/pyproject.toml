[project]
name = "flow_test"
version = "0.1.0"
description = "flow_test using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.14.4"
]

[project.scripts]
kickoff = "flow_test.main:kickoff"
run_crew = "flow_test.main:kickoff"
plot = "flow_test.main:plot"
run_with_trigger = "flow_test.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "flow"
